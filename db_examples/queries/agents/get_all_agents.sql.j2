-- Get All Agents Query
-- Retrieves all agents from the agents table
-- 
-- Purpose: Simple query to fetch all agent records
-- 
-- Returns: All columns and rows from the agents table
-- 
-- Columns returned:
--   - id: Unique identifier for the agent
--   - name: Agent's name
--   - age: Agent's age (must be > 1)
--   - memory_size: Agent's memory capacity (must be >= 3)
--
-- Example usage:
--   SELECT * FROM agents;
--
-- Note: This is a basic query that returns all agents without filtering.
-- Consider using more specific queries for production use cases.

SELECT * FROM agents;